{
  "name": "@mypl/geronte",
  "version": "0.12.1",
  "private": true,
  "packageManager": "npm@9.5.0",
  "workspaces": [
    "front/app"
  ],
  "scripts": {
    "dev": "dotenv turbo run dev",
    "build": "dotenv turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "dotenv-cli": "^6.0.0",
    "prettier": "^2.8.7",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  }
}
